# HW07 – Report

> Файл: `homeworks/HW07/report.md`  
> Важно: не меняйте названия разделов (заголовков). Заполняйте текстом и/или вставляйте результаты.

## 1. Datasets

Вы выбрали 3 датасета из 4 (перечислите):
S07-hw-dataset-01.csv
S07-hw-dataset-02.csv
S07-hw-dataset-03.csv

### 1.1 Dataset A

- Файл: `S07-hw-dataset-01.csv`
- Размер: (12000, 9)
- Признаки: числовые 
- Пропуски: нет
- "Подлости" датасета: нет

### 1.2 Dataset B

- Файл: `S07-hw-dataset-01.csv`
- Размер: (8000, 4)
- Признаки: числовые 
- Пропуски: нет
- "Подлости" датасета: нет

### 1.3 Dataset C

- Файл: `S07-hw-dataset-01.csv`
- Размер: (15000, 5)
- Признаки: числовые 
- Пропуски: нет
- "Подлости" датасета: нет

## 2. Protocol

Опишите ваш "честный" unsupervised-протокол.

- Препроцессинг: scaling, pca
- Поиск гиперпараметров:
  - kmeans_ks: (2, '...', 20)
    dbscan eps: [1.5, 2.0, 2.5, 3.0, 3.5]
    dbscan min_samples: [3, 5, 10]
    agg ks: (2, '...', 20)
    agg linkages: ['ward', 'complete', 'average']
  - silhouette
- Метрики: silhouette, Davies-Bouldin, Calinski-Harabasz 
- Визуализация: PCA(2D)

## 3. Models

Перечислите, какие модели сравнивали **на каждом датасете**, и какие параметры подбирали.

Минимум (для каждого датасета):

- KMeans (поиск `k`, фиксировали `random_state`, `n_init`)
- DBSCAN (`eps`, `min_samples`, доля шума)
- AgglomerativeClustering (`k`, `linkage`)


## 4. Results

Для каждого датасета – краткая сводка результатов.

### 4.1 Dataset A

- Лучший метод и параметры: kmeans k = 2
- Метрики (silhouette / DB / CH): "silhouette": 0.5216395622404244,
      "davies_bouldin": 0.6853295219054455,
      "calinski_harabasz": 11786.954622671537,
- Метрики согласованы — кластеризация качественная, KMeans подходит для структуры данных в df1

### 4.2 Dataset B

- Лучший метод и параметры: kmeans k = 2
- Метрики (silhouette / DB / CH): "silhouette": "silhouette": 0.30686100177016024,
      "davies_bouldin": 1.3234721699867633,
      "calinski_harabasz": 3573.3933329348465,
- Метрики согласованы — кластеризация качественная, KMeans подходит для структуры данных в df2

### 4.3 Dataset C

- Лучший метод и параметры: kmeans k = 3
- Метрики (silhouette / DB / CH): "silhouette": 0.31553248183109256,
      "davies_bouldin": 1.1577832240211146,
      "calinski_harabasz": 6957.158106946739,
- Метрики согласованы — кластеризация качественная, KMeans подходит для структуры данных в df3

## 5. Analysis

### 5.1 Сравнение алгоритмов (важные наблюдения)

- KMeans ломается: на несферичных кластерах и выбросах, т.к. предполагает сферичность и равный размер.
- DBSCAN/иерархическая выигрывают: когда кластеры произвольной формы или неизвестно их число.
- Сильнее всего влияло: масштабирование (для KMeans) и выбросы (ломают центроиды).

### 5.2 Устойчивость (обязательно для одного датасета)

- 5 запусков KMeans с разными random_state (42, 0, 100, 999, 77).
- Метрики (silhouette, D-B, C-H) не отличаются
Центроиды не смещаются 
Назначение кластеров объектам совпадает 
- Вывод: устойчиво

### 5.3 Интерпретация кластеров

- Как вы интерпретировали кластеры:
  - профили признаков (средние/медианы)
- Кластеры демонстрируют выраженную внутреннюю однородность — объекты внутри кластера похожи по набору ключевых характеристик. Наблюдаются значимые межкластерные различия — каждый кластер обладает уникальным "профилем", где одни признаки завышены, а другие занижены относительно среднего.

## 6. Conclusion

Метрики кластеризации противоречивы — нужно смотреть несколько (silhouette, D-B, CH) и визуализацию, т.к. оценивают разное.

Предобработка — масштабирование обязательно для KMeans, выбросы убивают центроидные алгоритмы.

Устойчивость проверяется — несколько запусков с разными seed показывают, не артефакт ли разбиение.

Интерпретация через профили — средние/медианы признаков в кластерах дают бизнес-смысл, иначе кластеризация бесполезна.